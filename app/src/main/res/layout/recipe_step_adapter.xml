<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/recipe_step_order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/normal_text_size"
        android:layout_centerVertical="true"
        tools:ignore="SpUsage" />

    <EditText
        android:id="@+id/input_recipe_step_description"
        android:layout_width="260dp"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:minLines="1"
        android:scrollbars="vertical"
        android:lines="3"
        android:maxLines="3"
        android:minWidth="200dp"
        android:layout_marginLeft="30dp"
        android:layout_toRightOf="@id/recipe_step_order"
        android:textSize="@dimen/normal_text_size"
        android:layout_marginStart="30dp"
        tools:ignore="SpUsage"
        android:layout_toEndOf="@id/recipe_step_order" />

    <ImageButton
        android:id="@+id/delete_recipe_step_button"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginLeft="5dp"
        android:background="#00FFFFFF"
        android:layout_centerVertical="true"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_remove_circle_outline_24px"
        android:layout_toRightOf="@id/input_recipe_step_description" />

</RelativeLayout>